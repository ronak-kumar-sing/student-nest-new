# 🏗️ StudentNest Mobile - Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                     StudentNest Mobile App                      │
│                   (React Native + Expo)                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                        App Layer                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │   (auth)     │  │   (tabs)     │  │  property    │        │
│  │  ┌────────┐  │  │  ┌────────┐  │  │  ┌────────┐  │        │
│  │  │ Login  │  │  │  │  Dash  │  │  │  │ Details│  │        │
│  │  │  OTP   │  │  │  │  Props │  │  │  │  Edit  │  │        │
│  │  └────────┘  │  │  │  Books │  │  │  └────────┘  │        │
│  └──────────────┘  │  │  Profile│  │  └──────────────┘        │
│                    │  └────────┘  │                            │
│                    └──────────────┘                            │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                    Component Layer                             │
│  ┌──────┐  ┌─────────┐  ┌────────┐  ┌────────┐  ┌─────────┐ │
│  │  UI  │  │Property │  │Booking │  │ Visit  │  │Dashboard│ │
│  │Button│  │  Card   │  │  Card  │  │  Card  │  │  Stats  │ │
│  │Input │  │  Form   │  │Timeline│  │Schedule│  │  Chart  │ │
│  │Card  │  │ Uploader│  │Actions │  │ Actions│  │  Feed   │ │
│  └──────┘  └─────────┘  └────────┘  └────────┘  └─────────┘ │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                     Context Layer                              │
│  ┌──────────────────┐         ┌──────────────────┐           │
│  │  AuthContext     │         │  ThemeContext    │           │
│  │  - user          │         │  - theme         │           │
│  │  - login()       │         │  - themeMode     │           │
│  │  - logout()      │         │  - setTheme()    │           │
│  │  - isAuth        │         │  - isDark        │           │
│  └──────────────────┘         └──────────────────┘           │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                      Hooks Layer                               │
│  ┌──────────┐  ┌───────────┐  ┌──────────┐  ┌────────────┐  │
│  │ useAuth  │  │ useTheme  │  │useUpload │  │useBookings │  │
│  └──────────┘  └───────────┘  └──────────┘  └────────────┘  │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                       API Layer                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              API Client (Axios)                        │   │
│  │  ┌──────────────────┐  ┌──────────────────┐           │   │
│  │  │    Interceptors  │  │  Error Handling  │           │   │
│  │  │  - Add Token     │  │  - Retry Logic   │           │   │
│  │  │  - Refresh Token │  │  - Error Toast   │           │   │
│  │  └──────────────────┘  └──────────────────┘           │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐│
│  │  Auth   │ │Property │ │Booking  │ │ Visit   │ │Payment ││
│  │ API     │ │  API    │ │  API    │ │  API    │ │  API   ││
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └────────┘│
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                     Storage Layer                              │
│  ┌──────────────────┐         ┌──────────────────┐           │
│  │  AsyncStorage    │         │   Cloudinary     │           │
│  │  - Tokens        │         │   - Images       │           │
│  │  - User Data     │         │   - Videos       │           │
│  │  - Preferences   │         │   - Documents    │           │
│  └──────────────────┘         └──────────────────┘           │
└────────────────────────────────────────────────────────────────┘
                              │
                              │
┌─────────────────────────────▼─────────────────────────────────┐
│                     Backend Services                           │
│  ┌────────────────────────────────────────────────────────┐   │
│  │     student-nest-new API                               │   │
│  │     https://student-nest-for.vercel.app/api            │   │
│  │                                                         │   │
│  │  ┌──────┐ ┌─────┐ ┌────────┐ ┌──────┐ ┌────────┐      │   │
│  │  │Auth  │ │Props│ │Bookings│ │Visits│ │Payments│      │   │
│  │  └──────┘ └─────┘ └────────┘ └──────┘ └────────┘      │   │
│  └────────────────────────────────────────────────────────┘   │
│                                                                │
│  ┌────────────────────────────────────────────────────────┐   │
│  │              MongoDB Atlas Database                    │   │
│  │  ┌──────┐ ┌──────┐ ┌────────┐ ┌──────┐ ┌────────┐    │   │
│  │  │Users │ │Rooms │ │Bookings│ │Visits│ │Payments│    │   │
│  │  └──────┘ └──────┘ └────────┘ └──────┘ └────────┘    │   │
│  └────────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. Authentication Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │ Enter email/password
     ▼
┌─────────────┐
│Login Screen │
└─────┬───────┘
      │ 1. Call login()
      ▼
┌──────────────┐
│ AuthContext  │
└──────┬───────┘
       │ 2. API request
       ▼
┌───────────────┐
│  API Client   │
└───────┬───────┘
        │ 3. POST /auth/login
        ▼
┌────────────────┐
│ Backend API    │
└────────┬───────┘
         │ 4. Validate credentials
         │ 5. Generate tokens
         ▼
┌─────────────────┐
│   Response      │
│ {user, token}   │
└────────┬────────┘
         │ 6. Store in AsyncStorage
         ▼
┌──────────────────┐
│  AsyncStorage    │
└────────┬─────────┘
         │ 7. Update context
         ▼
┌──────────────────┐
│   Navigate to    │
│    Dashboard     │
└──────────────────┘
```

### 2. Property Creation Flow

```
┌──────────┐
│  Owner   │
└────┬─────┘
     │ Fill form
     ▼
┌─────────────────┐
│ Property Form   │
└────┬────────────┘
     │ 1. Select images
     ▼
┌─────────────────┐
│ ImagePicker     │
└────┬────────────┘
     │ 2. Upload to Cloudinary
     ▼
┌─────────────────┐
│  Cloudinary     │
└────┬────────────┘
     │ 3. Get URLs
     ▼
┌─────────────────┐
│ Form Component  │
└────┬────────────┘
     │ 4. Submit data
     ▼
┌─────────────────┐
│  API Client     │
└────┬────────────┘
     │ 5. POST /rooms
     ▼
┌─────────────────┐
│  Backend API    │
└────┬────────────┘
     │ 6. Save to DB
     ▼
┌─────────────────┐
│   MongoDB       │
└────┬────────────┘
     │ 7. Return success
     ▼
┌─────────────────┐
│  Show success   │
│  Navigate back  │
└─────────────────┘
```

### 3. Booking Management Flow

```
┌──────────┐
│  Owner   │
└────┬─────┘
     │ View bookings
     ▼
┌──────────────────┐
│ Bookings Screen  │
└────┬─────────────┘
     │ 1. Fetch bookings
     ▼
┌──────────────────┐
│   API Client     │
└────┬─────────────┘
     │ 2. GET /bookings
     ▼
┌──────────────────┐
│   Backend API    │
└────┬─────────────┘
     │ 3. Get from DB
     ▼
┌──────────────────┐
│   Display list   │
└────┬─────────────┘
     │ 4. Click booking
     ▼
┌──────────────────┐
│ Booking Details  │
└────┬─────────────┘
     │ 5. Accept/Reject
     ▼
┌──────────────────┐
│   Confirmation   │
└────┬─────────────┘
     │ 6. POST /bookings/:id/actions
     ▼
┌──────────────────┐
│   Backend API    │
└────┬─────────────┘
     │ 7. Update status
     │ 8. Send notification
     ▼
┌──────────────────┐
│   Show success   │
│   Refresh list   │
└──────────────────┘
```

### 4. Theme Switching Flow

```
┌──────────┐
│  User    │
└────┬─────┘
     │ Toggle theme
     ▼
┌──────────────────┐
│  Settings/UI     │
└────┬─────────────┘
     │ 1. Call setThemeMode()
     ▼
┌──────────────────┐
│  ThemeContext    │
└────┬─────────────┘
     │ 2. Update state
     │ 3. Save to AsyncStorage
     ▼
┌──────────────────┐
│  AsyncStorage    │
└────┬─────────────┘
     │ 4. Trigger re-render
     ▼
┌──────────────────┐
│  All Components  │
│  useTheme()      │
└────┬─────────────┘
     │ 5. Get new theme
     ▼
┌──────────────────┐
│   Apply new      │
│   colors & styles│
└──────────────────┘
```

---

## Component Hierarchy

```
App
├── ThemeProvider
│   └── AuthProvider
│       ├── Stack Navigation
│       │   ├── (auth) Layout
│       │   │   ├── Login Screen
│       │   │   ├── Signup Screen
│       │   │   └── OTP Screen
│       │   │
│       │   ├── (tabs) Layout
│       │   │   ├── Dashboard Screen
│       │   │   │   ├── StatsCard
│       │   │   │   ├── RevenueChart
│       │   │   │   └── ActivityFeed
│       │   │   │
│       │   │   ├── Properties Screen
│       │   │   │   ├── PropertyCard
│       │   │   │   ├── SearchBar
│       │   │   │   └── FilterButtons
│       │   │   │
│       │   │   ├── Bookings Screen
│       │   │   │   ├── BookingCard
│       │   │   │   ├── StatusFilter
│       │   │   │   └── SortOptions
│       │   │   │
│       │   │   └── Profile Screen
│       │   │       ├── ProfileHeader
│       │   │       ├── SettingsSection
│       │   │       └── LogoutButton
│       │   │
│       │   └── Property Details
│       │       ├── ImageGallery
│       │       ├── PropertyInfo
│       │       ├── AmenitiesList
│       │       └── EditButton
│       │
│       └── Modals
│           ├── Add Property Modal
│           ├── Edit Property Modal
│           └── Confirmation Modal
│
└── StatusBar
```

---

## File Organization

```
StudentNest/
│
├── app/                              # Routes (Expo Router)
│   ├── (auth)/                       # Auth group
│   │   ├── _layout.tsx              # Auth layout
│   │   ├── login.tsx                # Login screen
│   │   ├── signup.tsx               # Signup screen
│   │   └── otp.tsx                  # OTP screen
│   │
│   ├── (tabs)/                       # Main app tabs
│   │   ├── _layout.tsx              # Tab layout
│   │   ├── index.tsx                # Dashboard
│   │   ├── properties.tsx           # Properties list
│   │   ├── bookings.tsx             # Bookings list
│   │   └── profile.tsx              # Profile
│   │
│   ├── property/                     # Property screens
│   │   ├── [id].tsx                 # Property details
│   │   └── [id]/edit.tsx            # Edit property
│   │
│   └── _layout.tsx                   # Root layout
│
├── components/                       # Reusable components
│   ├── ui/                          # Basic UI
│   │   ├── Button.tsx
│   │   ├── Input.tsx
│   │   ├── Card.tsx
│   │   ├── Modal.tsx
│   │   ├── Badge.tsx
│   │   └── Avatar.tsx
│   │
│   ├── property/                    # Property-specific
│   │   ├── PropertyCard.tsx
│   │   ├── PropertyForm.tsx
│   │   ├── ImageUploader.tsx
│   │   └── PropertyStats.tsx
│   │
│   ├── booking/                     # Booking-specific
│   │   ├── BookingCard.tsx
│   │   ├── BookingTimeline.tsx
│   │   └── BookingActions.tsx
│   │
│   ├── visit/                       # Visit-specific
│   │   ├── VisitCard.tsx
│   │   └── VisitScheduler.tsx
│   │
│   ├── dashboard/                   # Dashboard widgets
│   │   ├── StatsCard.tsx
│   │   ├── RevenueChart.tsx
│   │   └── ActivityFeed.tsx
│   │
│   └── shared/                      # Shared components
│       ├── Header.tsx
│       ├── LoadingSpinner.tsx
│       └── EmptyState.tsx
│
├── lib/                             # Core functionality
│   ├── api/
│   │   ├── client.ts               # Axios client
│   │   └── cloudinary.ts           # Cloudinary upload
│   │
│   ├── utils/
│   │   ├── formatters.ts           # Format functions
│   │   ├── validators.ts           # Validation
│   │   └── helpers.ts              # Helper functions
│   │
│   └── constants/
│       └── api-urls.ts             # API endpoints
│
├── contexts/                        # React contexts
│   ├── AuthContext.tsx             # Auth state
│   └── ThemeContext.tsx            # Theme state
│
├── hooks/                           # Custom hooks
│   ├── useAuth.ts                  # Auth hook
│   ├── useTheme.ts                 # Theme hook
│   ├── useProperties.ts            # Properties hook
│   ├── useBookings.ts              # Bookings hook
│   └── useUpload.ts                # Upload hook
│
├── types/                           # TypeScript types
│   ├── auth.ts                     # Auth types
│   ├── property.ts                 # Property types
│   ├── booking.ts                  # Booking types
│   ├── visit.ts                    # Visit types
│   └── api.ts                      # API types
│
├── constants/                       # App constants
│   └── theme.ts                    # Theme config
│
├── assets/                          # Static assets
│   └── images/                     # Images
│
├── .env                            # Environment variables
├── app.json                        # Expo config
├── package.json                    # Dependencies
├── tsconfig.json                   # TypeScript config
│
└── Documentation/
    ├── README.md                   # Main docs
    ├── IMPLEMENTATION_GUIDE.md     # Code guide
    ├── PHASE_1_SETUP.md           # Phase 1
    ├── QUICK_START.md             # Quick start
    ├── API_REFERENCE.md           # API docs
    └── ARCHITECTURE.md            # This file
```

---

## Technology Stack Details

### Frontend Stack
```
┌─────────────────────────────────┐
│     Expo 54.0 Framework         │
│  ┌───────────────────────────┐  │
│  │   React Native 0.81.4     │  │
│  │  ┌─────────────────────┐  │  │
│  │  │  TypeScript 5.9     │  │  │
│  │  └─────────────────────┘  │  │
│  └───────────────────────────┘  │
└─────────────────────────────────┘
```

### Navigation Stack
```
┌──────────────────────────┐
│    Expo Router 6.0       │
│  (File-based routing)    │
│  ┌────────────────────┐  │
│  │ React Navigation   │  │
│  │  - Bottom Tabs     │  │
│  │  - Stack Navigator │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

### State Management
```
┌──────────────────────────┐
│    React Context API     │
│  ┌────────────────────┐  │
│  │  - AuthContext     │  │
│  │  - ThemeContext    │  │
│  └────────────────────┘  │
│                          │
│    React Hooks           │
│  ┌────────────────────┐  │
│  │  - useState        │  │
│  │  - useEffect       │  │
│  │  - useContext      │  │
│  │  - Custom hooks    │  │
│  └────────────────────┘  │
└──────────────────────────┘
```

### API & Storage
```
┌──────────────────────────┐
│       Axios              │
│  (HTTP Client)           │
└──────────────────────────┘
          │
          ▼
┌──────────────────────────┐
│    AsyncStorage          │
│  (Local Storage)         │
└──────────────────────────┘
          │
          ▼
┌──────────────────────────┐
│     Cloudinary           │
│  (Image Storage)         │
└──────────────────────────┘
```

---

## Security Architecture

```
┌──────────────────────────────────────────┐
│         Security Layers                  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  1. Authentication Layer           │  │
│  │     - JWT tokens                   │  │
│  │     - Token refresh                │  │
│  │     - Secure storage               │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  2. Authorization Layer            │  │
│  │     - Role-based access (owner)    │  │
│  │     - Route guards                 │  │
│  │     - API endpoint validation      │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  3. Data Protection Layer          │  │
│  │     - HTTPS only                   │  │
│  │     - Encrypted AsyncStorage       │  │
│  │     - Secure API calls             │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  4. Input Validation Layer         │  │
│  │     - Form validation              │  │
│  │     - Type checking                │  │
│  │     - Sanitization                 │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

---

## Performance Optimization

```
┌──────────────────────────────────────────┐
│     Performance Strategies               │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  1. Code Splitting                 │  │
│  │     - Lazy loading                 │  │
│  │     - Dynamic imports              │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  2. Image Optimization             │  │
│  │     - Cloudinary transforms        │  │
│  │     - Lazy image loading           │  │
│  │     - Proper image sizes           │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  3. API Optimization               │  │
│  │     - Request caching              │  │
│  │     - Debounced searches           │  │
│  │     - Pagination                   │  │
│  └────────────────────────────────────┘  │
│                                          │
│  ┌────────────────────────────────────┐  │
│  │  4. Rendering Optimization         │  │
│  │     - React.memo                   │  │
│  │     - FlatList for long lists      │  │
│  │     - Avoid unnecessary re-renders │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘
```

---

**Last Updated:** January 10, 2025
**Version:** 1.0.0
